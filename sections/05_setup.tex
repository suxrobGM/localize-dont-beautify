\section{Experimental Setup}
% Models table (tables/setup_models.tex); data table from manifest; Cartesian bake-off
% (faces x models x procedures x controls), fixed seed; provenance (git sha, run configs);
% cost/latency; one sentence on the FLUX.2 exclusion.
\todo{experimental setup}
