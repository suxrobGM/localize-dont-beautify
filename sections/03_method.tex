\section{Method}
% 3.1 control ladder (prompt-only -> masked composite -> masked inpaint)
% 3.2 two-tier landmarks + procedure->region masks
% 3.3 masked composite: align, feather, pose gating (yaw > 0.35)
% 3.4 prompt guardrails (surgical, not beauty-filter)
% Fig. 2: pipeline figure from run intermediates.
\todo{method}
